[package]
name = "shell"
version.workspace = true
edition.workspace = true

[dependencies]
driver = { path = "../driver" }
common = { path = "../common" }

[lib]
crate-type = ["rlib"]
path = "src/lib.rs"
